[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "javascript-related-papers"
authors = [{name = "Dezhen Kong", email = "mail@dezhen.io"}]
dynamic = ["version", "description"]
